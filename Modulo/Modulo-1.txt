1) Para instalar o aws cli digitar 

    1.1) Para instalar a AWS CLI, execute os comandos a seguir.
         $ curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
    1.2) depois caso não tenha instalar o unzip com o comando apt install unzip
    1.3) Digitar unzip awscliv2.zip para descompactar
    1.4) Digitar sudo ./aws/install

2) Após criar uma acess key ir no console linux e digitar ou IAM

   Para que serve uma Access Key na AWS?
   A Access Key (composta por um Access Key ID e um Secret Access Key) 
   é usada para autenticar e autorizar um usuário ou aplicação a acessar os serviços da AWS programaticamente,
    ou seja, fora do console web.

   2.0-> vá em IAM entre em user clique no usuário e na parte de security defina uma chave

   2.1) aws configure
   2.2) digitar a acess key
   2.3) digitar a secret access key
   2.4) a região na parte superior do ecrã ver a região e ver o código dela
        ex: United States (N. Virginia) é us-east-1
   2.5) em deafult output format pode digitar text ou json 'eu prefiro json'
   2.6) para verificar se está tudo ok digitar aws configure list
   2.7) para ver a configuração feita digite  cat ~/.aws/config~
   2.8) para ver a access key e a secrete access key digitar cat ~/.aws/credentials
   2.9) em ambos os comando acima terá como nome default se quiser mudar antes de criar pode digitar 
        aws configure --profile [nome]
3) validar sem uma access key é realmente verdadeira
   3.1) digitar no linux o comando aws sts get-caller-identity

4) Para ativar as visualizações de billing nas contas não root
   4.1) Entrar como root 
   4.2) clicar em account-id> depois em account 
   4.3) rolar a barra de rolagem até 'IAM user and role access to Billing information' e clicar em Edit
   4.4) e marcar a opção Activate IAM Access

  EC2

5) Para ver as recomendações do SavePlans mudou agora está em  
   5.1)  clicar em account-id> depois em account 
   5.2)  depois clicar em Billing and Cost Management
   5.2)  do lado esquerdo na barra de rolagem procurar Savings Plans
   5.3)  recomendações

  KEY PAR

6) pode se criar uma keypar quando se vai criar uma instância
   6.1) Dentro do linux é bom alterar a chave para chmod 400. para apenas o root poder alterar por uma questão de segurança.
   6.2) Você pode também criar uma keypar da seguinte forma:
   6.3) ssh-keygen -t rsa -b 2048 -f [nome da chave] 
   6.4) depois digitar ls [nome da chave criada]* 
   6.5) depois digitar cat [nome da chave criada].pub e copiar
   6.6) depois na hora da criação de keypar na aws selecionar importar e colar a chave do passo 6.5
   6.7) na hora de criar a instância essas chaves criadas irão aparecer para ser selecionadas

   Após criar uma máquina virtual

7) na lista de instância clicar na máquina virtual desejada

  VIA WINDOWS
   7.1) em details procurar por Public IPv4 address e copiar o IPv4
   7.2) depois digitar o comando: telnet ip 22 e verificar se vai conectar
   7.3) se conectar ao telnet, abrir o putty digitar o ip em host name
   7.4) depois ir em connection->SSH->AUth->credentials e importe a chave criada
   7.5) voltar para session digitar o ip e depois clicar em open
   7.6) depois para logar na máquina digitar ec2-user e ver se conectou na m+aquina

  VIA LINUX
   7.7) abrir um terminal linux e digitar ssh -i [arquivo].ppk ec2-user@[Ip da maquina EC2]
   7.8) vai aparecer uma msg de fringerprint digite yes
   7.9) se der bad permissions permission denied pois qualquer um pode mudar as permissões
   7.10) mude a permissão para chmod 400 [arquivo].ppk
   7.11) Load key "chave04ppk.ppk": error in libcrypto ela tem de ser convertida para .pem pois está no linux
   7.12) para converter de .ppk para pem digitar sudo puttygen [arquivo].ppk -O private-openssh -o [arquivo].pem
   7.13) sudo chmod 400 [arquivo].pem
   7.14) depois digitar para  ssh -i [arquivo].pem ec2-user@18.205.238.240

  AMI AMI 'Amazon Machine Image'

   8.1) Quando você entra em EC2->Instances->POde escolher já prontas
   8.2) Ou no passo 81.) tem a opçao Browser mor AMIs
   8.3) ou clicar em EC2 e no menu do lado esquerdo procurar por Images->AMI ou Images->AMI catalog
   8.4) Após criar uma AMI ir na lista clicar com o direito e ir em image and templates e selecionar create image

    Passo a passo para instalar MongoDB 7.0 no Amazon Linux 2023
        1. Atualize o sistema
          sudo dnf update -yShow more lines
2. Importe a chave GPG do MongoDB
Shellsudo rpm --import https://www.mongodb.org/static/pgp/server-7.0.ascShow more lines
3. Crie o repositório MongoDB
Shellecho "[mongodb-org-7.0]name=MongoDB Repositorybaseurl=https://repo.mongodb.org/yum/amazon/2023/mongodb-org/7.0/x86_64/gpgcheck=1enabled=1gpgkey=https://www.mongodb.org/static/pgp/server-7.0.asc" | sudo tee /etc/yum.repos.d/mongodb-org-7.0.repoShow more lines
4. Instale o MongoDB
ShellShow more lines
5. Inicie e habilite o serviço
Shellsudo systemctl start mongodsudo systemctl enable mongodShow more lines
6. Verifique o status
Shellsudo systemctl status mongodShow more lines

🔒 Segurança (opcional, mas recomendado)
Habilitar autenticação:

Edite o arquivo de configuração:
Shellsudo nano /etc/mongod.confShow more lines

Adicione:
YAMLsecurity:  authorization: enabledShow more lines

Reinicie o MongoDB:
Shellsudo systemctl restart mongodShow more lines



Se quiser instalar outra versão (como 6.0 ou 8.0), posso te passar os comandos específicos. Quer seguir com a 7.0 ou prefere outra versão?
Sources



  